# 自动画中画 · Auto PiP on Tab Switch

一个 Chrome 扩展（Manifest V3）：**视频播放中切换标签页时打开画中画，返回时自动退出，可以反复进行。**

## 它做了什么

| 你的操作 | 视频正在播放 | 视频已暂停 | 说明 |
| --- | --- | --- | --- |
| 切到**其他标签页** | ✅ 自动进入画中画 | ⬜ 不处理 | 核心行为 |
| 切回原标签页 | ⬜（可选择自动退出画中画） | ⬜ | 见「返回标签页时自动退出」 |
| 切到**其他应用**（⌘Tab / Alt+Tab） | ⬜ 不处理 | ⬜ 不处理 | 明确不触发 |
| 最小化浏览器窗口 | ⬜ 不处理 | ⬜ 不处理 | 不属于切标签页 |

「切标签页」和「切应用」的区分不是靠窗口焦点，而是靠**当前标签页是否还是所在窗口的活跃标签页**——切应用时活跃标签页不会变化，所以天然不会误触发。

## 安装

1. 打开 `chrome://extensions/`
2. 打开右上角的 **开发者模式**
3. 点 **加载已解压的扩展程序**，选择本目录（含 `manifest.json` 的那一层）
4. 建议把图标固定到工具栏，方便随时开关

> 使用支持 Manifest V3 和视频画中画 API 的桌面 Chrome。
> 自动浮窗由 Chrome 自己的「自动画中画」机制触发，需要 Chrome 对该网站放行（见下文「连续自动浮窗的前提」）。
> 实际浮窗行为请在正常浏览器窗口中验证。

## 控制面板

点击工具栏图标打开，两个开关都保存在 `chrome.storage.sync`（会跟随账号同步）：

| 开关 | 默认 | 作用 |
| --- | --- | --- |
| **切换标签页时进入画中画** | 开 | 总开关。关掉后完全不再自动浮窗 |
| **返回标签页时自动退出** | 开 | 回到该标签页时把视频收回页面内 |

面板顶部还会显示当前标签页的状态（正在播放 / 已在画中画中 / 未检测到视频），底部按钮可手动进入或退出画中画。

历史界面预览（浅色 / 深色跟随系统；新版已用权限提示替换兼容模式）：

| 浅色 | 深色 |
| --- | --- |
| ![浅色](docs/preview-light.png) | ![深色](docs/preview-dark.png) |

## 工作原理

普通 `requestPictureInPicture()` 需要**用户激活**，而那个激活是一次性的：第一次自动浮窗用掉之后，返回再切走就没有激活可用了。所以这里不再反复调用它，改用 Chrome 内置的**自动画中画**（auto picture-in-picture）机制——它由浏览器自己发起，因此可以反复触发。

浏览器在决定自动浮窗时会先问一句：页面此刻注册了 `enterpictureinpicture` 动作处理器吗？

- **注册了**：浏览器把动作派发给页面，由页面自己决定。我们完全让路——不替换、不包裹、不改写它的处理器，也不替它决定要不要浮窗。
- **没注册**：浏览器默认自己开窗。这种情况下由我们注册的处理器顶替，在派发时被调用；而浏览器在派发前会给页面一次**用户激活**，它跨任务依然有效，于是我们在这里调用 `requestPictureInPicture()` 就不会被拒——反复多次也一样。

为了在两条路径之间正确判定，扩展在 `document_start` 就往页面世界（MAIN world）注入一个桥接脚本（`native-pip-bridge.js`），在任何网站代码之前接管 `MediaSession.prototype.setActionHandler`。它只观察网站注册了什么，每一次调用都原样转发给浏览器；判定结果通过 DOM 事件交给内容脚本，由内容脚本按开关决定进出。

回到标签页时的退出是扩展自己的行为（`document.exitPictureInPicture()`），不需要用户激活。

### 连续自动浮窗的前提

这几条是 Chrome 源码里写死的资格条件，缺一条就不会触发：

1. **网站允许自动画中画。** 在地址栏左侧的网站设置（或 `chrome://settings/content/autoPictureInPicture`）里把它设为「允许」。首次触发时 Chrome 会弹出提示，允许之后才会持续生效。
2. **页面必须是 `https://` 或 `file://`。** Chrome 明确拒绝其它协议，所以本地 `http://127.0.0.1` 上的页面永远不会触发自动画中画。
3. **媒体真的在播放、真的出声。** 需要当前标签页持有音频焦点并且最近有声音；静音、无音轨或纯画面动画都不算。
4. **播放器在顶层框架里。** 自动画中画的判定只针对主框架。
5. **URL 安全检查已通过。** Chrome 会先做一次异步的网址安全检查，通过后才进入判定。

### 其它已知限制

快速返回或主动退出会取消尚未完成的进入请求。用户手动关闭浮窗后，本轮隐藏期间禁止自动重开；退出失败时面板显示错误名称。

扩展不接管网站的媒体控制，也不改变视频右键菜单的行为。若网站在我们之后注册了自己的 `enterpictureinpicture` 处理器，我们立刻把控制权交还给它，并且不会清掉它。

### 从有问题的版本恢复

1. 在 `chrome://extensions/` 重新加载本扩展。
2. **刷新所有曾打开视频的标签页**，清除旧版已经注入的媒体会话回调。只重新加载扩展不会移除旧页面的回调。
3. 确认视频画面回到页面，再使用网站自身的画中画按钮测试进入和退出。
4. 如果刷新后仍卡在黑屏/画中画占位状态，关闭该视频标签页并重新打开。新页面恢复前不要重复触发画中画。

## 其他限制

**2. 跨域 iframe 里的播放器**
如果播放器在 iframe 里且 iframe 没有授权 `allow="picture-in-picture"`，浏览器会直接禁止画中画——这属于页面自身的权限策略，扩展无法绕过。

**3. DRM 视频**
Netflix 等受 DRM 保护的内容可能拒绝进入画中画。

**4. 切换到另一个 Chrome 窗口**
本扩展把「切标签页」定义为**同一个窗口内换了活跃标签页**。若你把视频窗口完整地留在屏幕上、只是切到另一个 Chrome 窗口，不会触发。

**5. 站点自己接管了自动画中画**
有些网站会给 `enterpictureinpicture` 注册自己的处理器来决定进退。这时浏览器直接把动作交给网站，扩展完全不介入，最终行为由网站决定。

## 文件结构

```
├── manifest.json         # MV3 清单：只申请 storage 权限 + 全站内容脚本
├── background.js         # Service Worker：判定「切标签页」还是「切应用」
├── native-pip-bridge.js  # MAIN world 桥接：接管媒体会话动作派发
├── content.js            # 内容脚本：找播放中的视频、进出画中画
├── popup.html/css/js     # 控制面板
├── icons/                # 16 / 32 / 48 / 128 图标
├── docs/                 # 控制面板预览图
├── package.json          # 构建脚本入口
├── build/                # 打包产物（已写入 .gitignore）
└── tools/
    ├── gen-icons.mjs     # 生成 icons/ 下的 PNG
    ├── package.mjs       # 打包成 zip
    └── lib/zip.mjs       # 手写 ZIP 写入器（仅用 node:zlib）
```

## 构建与打包

扩展的构建脚本只用 Node 标准库；运行单元测试无需安装依赖。浏览器 E2E 使用 Playwright 1.63（需要 Node.js 20+），需要先安装开发依赖和测试用 Chromium：

```bash
npm test          # 事件、短暂激活和异步竞争单元测试
npm install       # 首次运行 E2E 时安装 Playwright
npm run e2e:install
npm run test:e2e  # Playwright 加载真实扩展并执行 Node 断言
npm run test:e2e:native  # 只跑 file:// 原生自动画中画多轮验证
npm run icons     # 重新生成 icons/ 下的四个 PNG
npm run package   # 打包成 build/auto-pip-on-tab-switch-<version>.zip
npm run build     # 上面两步一起跑
```

E2E 默认使用 headed Chromium，以便验证系统画中画窗口；整个流程只通过 Playwright 和 Node 断言，不依赖人工操作或目测结果。两个 E2E 文件串行执行（`--test-concurrency=1`）：并行打开两个有窗口的浏览器会互相抢前台，导致标签页/应用切换信号失真。
测试使用独立的临时浏览器配置，结束后自动清理。仅在环境确认支持扩展和画中画的新无头模式时，才设置 `PLAYWRIGHT_HEADLESS=1`。

`tests/e2e/native-autopip.e2e.mjs` 是连续自动浮窗的核心证据：它用 `file://` fixture、一次真实点击产生的带音轨媒体，以及只关掉用户激活的 CDP 采样，验证「切走 → 浏览器自动浮窗 → 返回退出」连续多轮成立，并且每次进入都确实经过扩展注册的处理器。它需要该 profile 打开扩展的「允许访问文件网址」。

打包走白名单（`tools/package.mjs` 里的 `INCLUDE`），只收进扩展运行时需要的文件，
README、docs、tools 都不会进包，产物可直接上传 Chrome 应用商店或解压分发。
若 `manifest.json` 引用了白名单里缺失的文件，打包会**报错退出**而不是产出一个坏包。
打包产物目录 `build/` 已写入 `.gitignore`。

排查问题时，可先在 `chrome://extensions/` 点扩展卡片上的刷新按钮，再重新加载出问题的页面。

## 恢复验证

- 视频能正常显示在页面中。
- 网站画中画按钮能进入，退出后画面回到页面。
- 插件能退出当前页面的视频画中画；失败时显示实际错误。
- 关闭浮窗后不会由插件立即重新打开。
- 反复切走/切回可以连续自动进入和退出（前提见「连续自动浮窗的前提」）。

单元测试只验证脚本状态控制，不能证明系统浮窗可见；系统浮窗的可见性由 `npm run test:e2e` 在真实浏览器里验收。
